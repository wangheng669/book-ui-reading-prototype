# 主动回忆生成规则

- 每个高价值知识点最多生成一个轻量问题；先要求用户用自己的话回答，再提供提示。
- 问题必须可由引用的 block 回答，不要求原文没有支持的外部知识。
- 每个问题定义 `requiredDimensions`；完整判定依赖多个知识维度，不使用单个关键词。
- 每个维度包含解释、证据 block 和可接受信号组；信号只用于离线原型，不等同语义评分。
- 错误反馈使用“已提及 / 可补充”，并提供返回原文的 block id。

