# Step 5 · 组件数据生成

读取 `rules/05-components.md` 和 Step 4 结果。

只为 `assistance.needed = true` 的知识点生成一种主要组件。所有条目必须引用 `blockIds`，内容只能重组原文明确支持的信息。

输出：`{ components: [{ blockId, knowledgePointId, componentType, componentData }] }`。
